# Copy to .env.development.local to override (file is gitignored via *.local).
# User Analytics — "true" uses GET /api/v1/superadmin/user-analytics/* (Super Admin token required).
VITE_USE_USER_ANALYTICS_API=true
